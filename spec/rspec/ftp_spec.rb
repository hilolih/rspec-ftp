require 'spec_helper'

describe Rspec::Ftp do
  it 'has a version number' do
    expect(Rspec::Ftp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
